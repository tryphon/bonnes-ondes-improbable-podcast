        <h2>Podcast</h2>

        <div class="podcast">
          <p>
            Adresse du podcast : <input size="{{show.url_for_podcast | size}}" value="{{show.url_for_podcast}}" type="text" readonly="true"/> 
          </p>
      
          <p>
            <a href="{{show.itpc_url_for_podcast}}" title="Ajouter cd podcast dans votre iTunes">iTunes</a> 
            | <a href="{{show.url_for_podcast}}" title="Voir le feed RSS">RSS</a> 
            | {{show.clippy_for_podcast}}
          </p>
        </div>

        <h2>Retrouvez nous via</h2>

        <ul class="social-medias">
          <li class="rss"><a href="{{show.url_for_podcast}}">Feed RSS</a></li>
          <li class="facebook"><a href="https://www.facebook.com/pages/Limprobable-podcast/353673997994138">FaceBook</a></li>
          <li class="twitter"><a href="http://twitter.com/improbablepodca">Twitter</a></li>
        </ul>

        <h2>Sur Twitter</h2>

        <div class="twitter">
          
        </div>

        <div>
          <span id="twitter-follow"/>

          <script type="text/javascript">
            twttr.anywhere(function (T) {
              T('#twitter-follow').followButton("improbablepodca");
            });
          </script>
        </div>

        {% for episode in show.tag.saison-1 %}
          {% if forloop.first %}
          <h2>Saison 1</h2>
          <ul class="episodes">
          {% endif %}
            <li>
              <a href="{{ episode.url_for }}">
              {{ episode.title }}
              </a>
              {% if episode.broadcasted_at %}
              <span class="broadcast_date">diffusé {{episode.broadcasted_at | date: 'le %d/%m/%Y'}}</span>
              {% endif %}
            </li>
          {% if forloop.last %}
          </ul>
          {% endif %}
        {% endfor %}


