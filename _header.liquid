      <div id="header">
        <h1 onclick="document.location  = '{{show.url_for}}'">{{show.name}}</h1>
        <span class="headline">
          {% for episode in show.episodes limit: 1 %}
          Dernier épisode<br/>
          <a href="{{ episode.url_for }}">
            <strong>{{episode.title}}</strong>
            <span class="date">le {{episode.broadcasted_at | date: '%d/%m'}}</span>
          </a>
          {% endfor %}
        </span>
      </div>
